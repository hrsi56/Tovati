# דו״ח מסירה — טובתי 1.6.3

תאריך בדיקה: 24 ביולי 2026
גרסה: `1.6.3` (`versionCode=12`)
מנוע: `bbt-fusion-2.3.1` (ללא שינוי)
מסד נתונים: Room v5 (ללא שינוי סכמה)
גיבוי: schema v5; שחזור v1–v5 (ללא שינוי)

## מה השתנה

- תוקן באג שבו עריכת מדידת חום קיימת ושינוי שדה התאריך שלה לתאריך אחר "הזיז" את הרשומה לתאריך החדש במקום ליצור רשומה נפרדת — ומחק בפועל את המדידה של התאריך המקורי. שמירה נפתרת עכשיו אך ורק לפי התאריך היעד: אם התאריך שונה, השמירה פוגעת ברשומה של התאריך החדש (או יוצרת חדשה) ולעולם לא נוגעת ברשומה שממנה יצאה העריכה.
- תוקן מצב שבו הדגל הפנימי שמסמן "שמירה בתהליך" לא התאפס לאחר שמירה מוצלחת (רק לאחר כישלון). אם מסך הזנת המדידה נשאר בזיכרון ונפתח שוב באותה הפעלה, השמירה הבאה הייתה נבלמת בשקט בלי הודעת שגיאה — זה מה שנראה כ"האפליקציה נתקעת ולא נותנת לשמור".

## תוצאות אימות

| בדיקה | תוצאה |
|---|---|
| בדיקות יחידה (`testDebugUnitTest`) | 151 מתוך 151 עברו |
| Lint (`lintDebug`) | 0 שגיאות; 72 אזהרות לא חוסמות (זהה לגרסה הקודמת) |
| בנייה חתומה (`assembleRelease`) | הצליחה |
| רגרסיית שמירה לפי תאריך יעד (עריכת מדידה והעברתה לתאריך אחר, תפוס או פנוי) | טסט ה־instrumented עודכן לבדיקת ההתנהגות הנכונה וקומפל בהצלחה |
| בדיקות Android instrumented (`connectedDebugAndroidTest`) ובדיקה ידנית במכשיר | **לא הורצו** — לא היה מכשיר/אמולטור מחובר בסביבת הפיתוח הנוכחית. מומלץ להתקין את הקובץ ולוודא ידנית לפני הפצה רחבה, בפרט את תרחיש "הזנת מדידה להיום, ואז לתאריך אחר" שתואר כבאג. |
| חתימת APK | APK Signature Scheme v2; חותם יחיד; אותה תעודה כמו בגרסאות הקודמות |

## קובץ המסירה

- קובץ גרסה: `Tovati-1.6.3.apk`
- קובץ הורדה קבוע לאתר: `Tovati.apk`
- גודל: 3,226,740 בתים
- SHA-256: `ab47b39e9b9b7af2dbce6043137ba10d77b1b819ced81b2167eb2b3b5e9d9c1f`
- SHA-256 של תעודת החתימה: `602369f2b13e64a1c95885ca5b576cfa947caf0824f80c4770a63bafa665bb6a`

להתקנה מעל גרסה קיימת אין להסיר את האפליקציה. מומלץ ליצור קודם גיבוי מוצפן.
